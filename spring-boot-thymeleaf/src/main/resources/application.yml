spring:
    # 配置前端Thymeleaf模板引擎
  thymeleaf:
  # 打包为jar末尾无/
    prefix: classpath:/templates
    check-template-location: true
    suffix: .html
    encoding: UTF-8
    servlet:
      content-type: text/html
    mode: HTML5
    # 禁止后实现前端热部署
    cache: false